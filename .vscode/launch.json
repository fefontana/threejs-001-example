{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Chrome Local",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:5500",
            "webRoot": "${workspaceFolder}"
        }
    ]
}